{
  "name": "memoir",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @memoir/demo dev",
    "build": "pnpm --filter @memoir/demo build",
    "check": "pnpm -r check",
    "test": "pnpm -r test"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
