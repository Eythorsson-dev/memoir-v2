lets implement a new feature:
I want to implement MVP support for undo/redo in the note editor. 
The toolbar should have buttons for Undo and redo. The buttons should be disabled if undo/redo is not possible
We should have a new History class that handles the state changes
